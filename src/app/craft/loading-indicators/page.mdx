import Loading from "./components/Loading";
import Card from "../../../components/card/Card";

export const metadata = {
  title: "Loading Indicators",
  description: "Simple loading state indicators.",
  publishedDate: "2023-11-11",
};

# Loading Indicators

Simple loading state indicators. A little detail is the two stacked animations, one `linear`, the other eased with a custom `ease-in-out` curve—giving the second and third spinners a bit more [juice](https://youtu.be/Fy0aCDmgnxg?si=dXJARNHwqKVE3PSe).

<Card style={{ display: "flex", gap: "8px" }}>
  <Loading variant="ios-spinner" />
  <Loading variant="primary-spinner" />
  <Loading variant="secondary-spinner" />
  <Loading variant="loading-dots" />
  <Loading variant="loading-text" />
</Card>
